<?xml version="1.0" encoding="utf-8"?>
<!--
  Local-only overrides. Copy this file to `test.runsettings.local` (which is gitignored) and edit.
  Used to override defaults (e.g. point at a different image tag, different port, different DB).
-->
<RunSettings>
  <TestRunParameters>
    <!-- Examples — uncomment and edit as needed:
    <Parameter name="vnext:imageTag"    value="0.0.32" />
    <Parameter name="vnext:runtimePort" value="4202" />
    <Parameter name="vnext:dbName"      value="vnext_{{domain}}_dev" />
    -->
  </TestRunParameters>
</RunSettings>
