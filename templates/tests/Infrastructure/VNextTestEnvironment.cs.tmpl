using VNext.Testing.Sdk;

namespace {{Domain}}.IntegrationTests.Infrastructure;

/// <summary>
/// Domain-specific test environment for the {{domain}} domain.
/// Spins up the Docker stack (runtime + MockLab + Dapr sidecar) and exposes a typed API client.
/// </summary>
public class VNextTestEnvironment : VNextTestEnvironmentBase
{
    public override string Domain => "{{domain}}";

    public override string DbName => "vnext_{{domain}}_test";

    public override string ImageTag => "latest"; // override locally via test.runsettings.local for a pinned tag

    // Override other hooks here as needed:
    // - SeedDirectory (MockLab seed mount)
    // - RuntimePort (default 4201)
    // - HostNetwork (default "bbt-development")
}
